{
    "name": "mafi-music-downloader",
    "version": "1.0.0",
    "description": "YouTube to Telegram MP3 Downloader",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "install-deps": "apt-get update && apt-get install -y python3 ffmpeg && pip3 install yt-dlp"
    },
    "dependencies": {
        "express": "^4.18.2",
        "node-telegram-bot-api": "^0.61.0",
        "yt-dlp-exec": "^1.0.2",
        "yt-search": "^2.10.4",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1"
    }
}
